import type { Themes } from 'types/styles/thems';

const defaultTheme = {
  colors: {
    primary: '#252525',
    window: '#808080'
  }
};

const themes: Themes = { defaultTheme };

export default themes;
